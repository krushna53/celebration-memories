# services/

Server-side data access layer (Supabase queries/mutations, wrapped as
typed functions consumed by Server Components / Server Actions). Populated
starting Phase 3 (RSVP + database wiring).
