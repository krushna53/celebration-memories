/**
 * Superseded — timeline milestones now live in the `timeline_milestones`
 * table, managed from /admin/timeline (see services/timeline.ts and
 * types/content.ts → TimelineMilestoneRecord). This file is kept only so
 * existing imports don't 404; it intentionally exports nothing.
 */
export {};
