/**
 * Superseded by features/auth/unified-login-form.tsx (one shared
 * sign-in form for admin/business/forms accounts) — app/business/login
 * now redirects to /login instead of rendering this component. Kept
 * as an empty, unimported stub rather than deleted: this sandbox's
 * FUSE mount can't remove files (same limitation documented in this
 * repo's CLAUDE.md for `testfile456`/git lock files). Safe to
 * `git rm features/business/login-form.tsx` next time this repo is
 * checked out somewhere with normal filesystem access.
 */
export {};
