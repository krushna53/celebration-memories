# styles/

Non-Tailwind global styling assets (e.g. print stylesheets, email
templates) if/when needed. `app/globals.css` remains the single source of
truth for the design tokens and Tailwind v4 theme.
